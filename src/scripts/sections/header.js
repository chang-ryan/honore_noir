/**
 * Header Section Script
 * -----------------------------------------------------------------------------
 * A file that contains scripts highly couple code to the Header section.
 *
 * @namespace header
 */

theme.Header = {
  var selectors = {
    menuButton: 'menu'
  };


};
